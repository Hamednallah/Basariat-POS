# Application Configuration
app.version=0.1.0-SNAPSHOT
app.name=Basariat POS

# Database Configuration
# These are illustrative and might be used by AppConfigLoader for other purposes.
# DBManager currently uses hardcoded values but could be updated to use these.
db.url=jdbc:postgresql://localhost:5433/basariat_pos_db
db.username=basariat_pos_user
db.password=POST

# Application specific settings
default.patient.id_prefix=PAT-
# Add other properties as needed, e.g., for UI settings, default report paths, etc.

# Locale settings (example)
# app.locale.default=en_US
# app.locale.supported=en_US,ar_SA

# External service integration (example, commented out)
# thirdparty.service.url=https://api.example.com/
# thirdparty.service.apikey=YOUR_API_KEY
# Logging configuration (though primarily handled by logback.xml)
# logging.level.com.basariatpos=DEBUG
# logging.file.path=logs/app.log
